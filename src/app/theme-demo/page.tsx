import ThemeDemoClient from "./ThemeDemoClient";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function InstitutionalThemeDemoPage() {
  return <ThemeDemoClient />;
}
